#include <stdio.h>

int main()
{
  char str[100],str2[100];
  int i,len;
  gets(str);
  len=strlen(str);
  for(i=0;i<len;i++)
  str2[i]=str[i];
  
 for(i=0;i<len;i++)
 printf("%c",str2[i]);
    return 0;
}

