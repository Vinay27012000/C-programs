#include <stdio.h>
#include <conio.h>
int main()
{
char str[100];
int len,i,count;

printf("\n\nenter string : ");
gets(str);
len=strlen(str);
for(i=len-1;i>=0;i--)
printf("%c",str[i]);

    return 0;
}

