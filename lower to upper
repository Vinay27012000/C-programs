#include <stdio.h>

int main()
{char str[100];
int len,i,asci;

    gets(str);
    len=strlen(str);
    for(i=0;i<len;i++){
        if(str[i]>='a'&&str[i]<='z')
        {asci=str[i];
        str[i]=asci-32;
        }
    }
    puts(str);

    return 0;
}

