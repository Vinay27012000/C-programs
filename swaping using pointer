#include <stdio.h>

int main()
{
    int swap(int*,int*);
    int x,y;
    scanf("%d%d",&x,&y);
    swap(&x,&y);
printf("%d %d",x,y);
    return 0;
}
int swap(int *p1,int *p2){
    int tmp;
    tmp=*p1;
    *p1=*p2;
    *p2=tmp;
    return (*p1,*p2);
}
